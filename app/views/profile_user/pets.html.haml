.col-md-9
  %h1= "Meus Pets"
  %table
    %tr
      %th Nome
      %th Aniversário
    - @pets.each do |pet|
      %tr
        %td= link_to pet.name, [pet.group, pet]
        - if pet.birthday
          %td= pet.birthday.strftime("%d/%m/%Y")
        %td= link_to 'Editar', edit_group_pet_path(pet.group, pet)
        %td= link_to 'Apagar', [pet.group, pet], :confirm => 'Tem certeza?', :method => :delete
