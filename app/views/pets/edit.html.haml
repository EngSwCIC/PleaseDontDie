.container
	%hr/
	.row
		.col-md-3
			= render 'layouts/sidemenu'
		.col-md-9
			%h1 Editando um Pet
			= render 'form'
			= link_to 'Mais', [@pet.group, @pet]
			|
			\#{link_to 'Voltar', group_pets_path(@pet.group)}
			%br/
			= link_to 'Novo Pet', new_group_pet_path(@group)
			|
			\#{link_to 'Voltar ao Grupo', group_path(@group)}